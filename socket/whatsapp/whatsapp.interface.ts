export enum WhatsappSocketRoomEnum {
    LISTEN_QR = "qr:whatsapp",
    LISTEN_READY = "ready:whatsapp",
    LISTEN_LOGOUT = "logout:whatsapp"
}

export enum EmitWhatsappSocketEnum {
    CHANGE_COMPANY = "change-company:whatsapp"
}